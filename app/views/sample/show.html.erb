<h1>Project <%= session[:project] %></h1>
sample: <%= @sample %><br />
params: <%= params %><br />
<%= image_tag('back.gif') %>
<%= link_to "Backto DataSet #{@data_set.name}", "/data_set/#{@data_set.id}", :method=>:get %>
<hr />
<h3>Samples</h3>
<% if @data_set %>
<table>
  <tr>
    <th></th>
    <% @data_set.headers.each do |header| %>
      <th><%= header %><%= link_to image_tag('minus.png'), "/sample/#{@data_set.id}/edit", :method=>:post %></th>
    <% end %>
    <th><%= link_to image_tag('add.png'), "/sample/#{@data_set.id}/edit", :method=>:post %></th>
  </tr>
  <% @data_set.samples.each_with_index do |sample, i| %>
    <tr>
      <td>
        <%= form_tag({:controller => 'sample',:action => 'edit'}) do %>
          <%= hidden_field :edit_option, :del_row, :value=>i.to_s %> 
          <%= image_submit_tag 'minus.png', :name=>'edit' %>
        <% end %>
      </td>
      <% @data_set.headers.each do |header| %>
        <td>
          <% file_path = sample.to_hash[header].to_s %>
          <% file_name = File.basename(file_path) %>
          <%= file_name %>
        </td>
      <% end %>
    </tr>
  <% end %>
</table>
  <%= form_tag({:controller => 'sample',:action => 'edit'}) do %>
    <%= hidden_field :edit_option, :add, :value=>'row' %> 
    <%= image_submit_tag 'add.png', :name=>'edit' %>
  <% end %>
  <%= button_to 'edit sample', "/sample/#{@data_set.id}/edit" %>
<% end %>
<hr />

